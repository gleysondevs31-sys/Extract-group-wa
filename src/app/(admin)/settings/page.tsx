"use client";import { useState } from "react";import { api } from "@/lib/api";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";import { toast } from "sonner";export default function Settings(){const [apiUrl,setApiUrl]=useState(process.env.NEXT_PUBLIC_BOT_API_URL||'');return <section className="max-w-3xl space-y-6"><h1 className="text-2xl font-semibold">Configurações</h1><Card><CardHeader><CardTitle>Bot Worker</CardTitle></CardHeader><CardContent className="space-y-4"><Input value={apiUrl} onChange={e=>setApiUrl(e.target.value)} placeholder="URL da API do Bot Worker"/><div className="grid gap-3 md:grid-cols-3"><Input defaultValue="5"/><Input defaultValue="600"/><Input defaultValue="1024"/></div><textarea className="min-h-28 w-full rounded-lg border bg-background p-3 text-sm" defaultValue="Use esta ferramenta apenas em grupos próprios, com autorização e consentimento, para finalidades internas permitidas."/><Button onClick={()=>api.health().then(()=>toast.success('Backend disponível')).catch(e=>toast.error(e.message))}>Testar conexão com backend</Button></CardContent></Card></section>}
