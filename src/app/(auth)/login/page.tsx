"use client";import { useForm } from "react-hook-form";import { zodResolver } from "@hookform/resolvers/zod";import { loginSchema } from "@/lib/schemas";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";import { toast } from "sonner";import { useRouter } from "next/navigation";import { z } from "zod";export default function Login(){const r=useRouter();const f=useForm<z.infer<typeof loginSchema>>({resolver:zodResolver(loginSchema)});async function onSubmit(v:z.infer<typeof loginSchema>){const res=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(v)});if(res.ok){toast.success('Login realizado');r.push('/dashboard')}else toast.error('Token inválido')}return <div className="grid min-h-screen place-items-center p-4"><Card className="w-full max-w-md"><CardHeader><CardTitle>Entrar no painel</CardTitle><p className="text-sm text-muted-foreground">Use o ADMIN_TOKEN configurado no ambiente.</p></CardHeader><CardContent><form onSubmit={f.handleSubmit(onSubmit)} className="space-y-4"><Input type="password" placeholder="Admin token" {...f.register('token')}/><Button className="w-full">Entrar</Button></form></CardContent></Card></div>}
