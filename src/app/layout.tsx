import type { Metadata } from "next";import { Toaster } from "sonner";import "./globals.css";export const metadata:Metadata={title:"WhatsApp Bot Admin",description:"Painel administrativo para Bot Worker Baileys"};export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="pt-BR"><body><Toaster richColors position="top-right"/>{children}</body></html>}
