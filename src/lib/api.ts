export type BotStatus={status:"connected"|"disconnected"|"connecting"|"error";phoneNumber?:string;lastConnection?:string;uptimeSeconds:number;baileysVersion:string;environment:string;error?:string};export type Participant={id:string;role:"Dono"|"Admin"|"Membro";number?:string;technicalId:string;type:"PN"|"LID"};export type GroupReport={id:string;groupName:string;totalParticipants:number;resolvedNumbers:number;hiddenLids:number;participants:Participant[];createdAt:string;status:string;errorMessage?:string};
async function request<T>(path:string,init?:RequestInit):Promise<T>{const r=await fetch(`/api/bot${path}`,{...init,headers:{"content-type":"application/json",...(init?.headers||{})},cache:"no-store"});if(!r.ok)throw new Error((await r.json().catch(()=>({error:"Erro"}))).error||"Erro na API");return r.json()}export const api={health:()=>request<{ok:boolean}>("/health"),status:()=>request<BotStatus>("/bot/status"),pairingCode:(phoneNumber:string)=>request<{code:string}>("/bot/pairing-code",{method:"POST",body:JSON.stringify({phoneNumber})}),disconnect:()=>request("/bot/disconnect",{method:"POST"}),logout:()=>request("/bot/logout",{method:"POST"}),analyze:(inviteUrl:string)=>request<GroupReport>("/groups/analyze",{method:"POST",body:JSON.stringify({inviteUrl})}),history:(q="")=>request<GroupReport[]>(`/groups/history${q}`),historyDetail:(id:string)=>request<GroupReport>(`/groups/history/${id}`),deleteHistory:(id:string)=>request(`/groups/history/${id}`,{method:"DELETE"})};
