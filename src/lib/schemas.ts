import { z } from "zod";export const loginSchema=z.object({token:z.string().min(8)});export const phoneSchema=z.object({phoneNumber:z.string().regex(/^\d{10,15}$/,"Use DDI e apenas números")});export const inviteSchema=z.object({inviteUrl:z.string().regex(/^https:\/\/chat\.whatsapp\.com\/[A-Za-z0-9]{20,}$/,"Link de convite inválido")});export const settingsSchema=z.object({apiUrl:z.string().url(),maxRunsPerHour:z.coerce.number().int().min(1).max(100),cooldownSeconds:z.coerce.number().int().min(0),maxGroupSize:z.coerce.number().int().min(1),legalNotice:z.string().min(20)});
