import { cookies } from "next/headers";import { NextResponse } from "next/server";const COOKIE="admin_session";export async function isAuthed(){return (await cookies()).get(COOKIE)?.value===process.env.ADMIN_TOKEN}export function setAuth(res:NextResponse){res.cookies.set(COOKIE,process.env.ADMIN_TOKEN||"",{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/"})}export function clearAuth(res:NextResponse){res.cookies.set(COOKIE,"",{maxAge:0,path:"/"})}
